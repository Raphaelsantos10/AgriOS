import{describe,expect,it}from"vitest";import type{HarvestRecord}from"../types/harvest";import{assessHarvestSafety,summarizeHarvestSafety}from"./harvest";const safe={id:"1",campaign:"2026",farm:"Q",field:"N",crop:"Tomate",harvestDate:"2026-08-01",harvestedAreaHa:1,grossQuantity:10,marketableQuantity:9,unit:"kg",quality:"first",destination:"Mercado",notes:"",lotCode:"L-1",hygieneChecked:true,containersClean:true,contaminationIncident:false,incidentNotes:"",safetyStatus:"released",releasedBy:"Ana",safetyEvidenceReference:"CHECK-1",createdAt:""}as HarvestRecord;
describe("segurança da produção vegetal",()=>{it("exige responsável e evidência antes de libertar o lote",()=>{const result=assessHarvestSafety({...safe,releasedBy:"",safetyEvidenceReference:""});expect(result.complete).toBe(false);expect(result.missing).toEqual(["responsável pela libertação","evidência da libertação"])});it("bloqueia lote com ocorrência e exige descrição",()=>{const result=assessHarvestSafety({...safe,contaminationIncident:true,incidentNotes:""});expect(result.blocked).toBe(true);expect(result.missing).toContain("descrição da ocorrência")});it("resume lotes libertados, pendentes e bloqueados",()=>{expect(summarizeHarvestSafety([safe,{...safe,id:"2",safetyStatus:"pending"},{...safe,id:"3",safetyStatus:"blocked"}])).toEqual({released:1,pending:1,blocked:1})})});
