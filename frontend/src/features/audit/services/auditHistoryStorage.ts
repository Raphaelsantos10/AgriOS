import type{AuditRun}from"../utils/auditHistory";import{appendAuditRun}from"../utils/auditHistory";const KEY="farpha.audit-history.v1";export function listAuditRuns():AuditRun[]{try{return JSON.parse(localStorage.getItem(KEY)??"[]")as AuditRun[]}catch{return[]}}export function saveAuditRun(run:AuditRun){const history=appendAuditRun(listAuditRuns(),run);localStorage.setItem(KEY,JSON.stringify(history));return history}
