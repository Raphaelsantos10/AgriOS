import{describe,expect,it}from"vitest";import{buildAuditItems,summarizeAudit,type AuditSnapshot}from"./auditAssistant";const snapshot={profileConfigured:true,overdueObligations:0,documents:2,expiredDocuments:0,treatments:1,treatmentWarnings:0,waterRequired:false,validWaterTitles:0,ifapRequired:true,ifapApplications:1,ifapMissingReceipts:0,fiscalPending:0,fiscalOverdue:0,insuranceAttention:0,harvestLots:1,traceableLots:1}as AuditSnapshot;
describe("assistente de auditoria",()=>{it("não exige água quando o perfil não indica captação",()=>{expect(buildAuditItems(snapshot).find(v=>v.id==="water")?.status).toBe("not_applicable")});it("sinaliza documentos vencidos e candidaturas sem comprovativo",()=>{const items=buildAuditItems({...snapshot,expiredDocuments:1,ifapMissingReceipts:1});expect(items.filter(v=>v.status==="attention").map(v=>v.id)).toEqual(["documents","ifap"])});it("calcula prontidão apenas sobre áreas aplicáveis",()=>{expect(summarizeAudit(buildAuditItems(snapshot))).toEqual({areas:8,ready:7,attention:0,notApplicable:1,score:100})})});
