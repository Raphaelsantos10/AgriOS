import{describe,expect,it}from"vitest";import type{AuditItem}from"./auditAssistant";import{buildAuditCsv,buildAuditPackageTextFiles,buildAuditReportHtml,safeFileName}from"./auditExport";const items=[{id:"x",area:"Água",title:"Título <TURH>",status:"attention",detail:'Falta "prova"',path:"/agua"}]as AuditItem[];
describe("exportação da pré-auditoria",()=>{it("protege o HTML contra conteúdo inserido pelo utilizador",()=>{const html=buildAuditReportHtml(items,"2026-07-21");expect(html).toContain("Título &lt;TURH&gt;");expect(html).not.toContain("<TURH>")});it("gera CSV escapando aspas e incluindo o aviso",()=>{const csv=buildAuditCsv(items,"2026-07-21");expect(csv).toContain('"Falta ""prova"""');expect(csv).toContain("não constitui certificação")});it("monta pacote técnico com nomes seguros",()=>{const files=buildAuditPackageTextFiles(items,{holder:"Quinta"},"2026-07-21");expect(Object.keys(files)).toEqual(["LEIA-ME.txt","relatorio-auditoria.html","checklist-auditoria.csv","dados-tecnicos.json"]);expect(safeFileName("Fatura água 1.pdf")).toBe("Fatura-agua-1.pdf")})});
