import type{AuditItem}from"./auditAssistant";import{summarizeAudit}from"./auditAssistant";
const esc=(v:string)=>v.replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");const csv=(v:string|number)=>`"${String(v).replaceAll('"','""')}"`;export function safeFileName(value:string){return value.normalize("NFD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-zA-Z0-9._-]+/g,"-").replace(/^-+|-+$/g,"")||"ficheiro"}
export function buildAuditCsv(items:AuditItem[],generatedAt:string){const rows=items.map(v=>[v.area,v.title,v.status,v.detail,v.path]);return [["Área","Verificação","Estado","Detalhe","Módulo"],...rows,["Relatório","Gerado em",generatedAt,"Resultado indicativo; não constitui certificação",""]].map(r=>r.map(csv).join(";")).join("\r\n")}
export function buildAuditReportHtml(items:AuditItem[],generatedAt:string){const s=summarizeAudit(items);return`<!doctype html><html lang="pt"><head><meta charset="utf-8"><title>FARPHA - Dossiê de auditoria</title><style>@page{size:A4;margin:16mm}body{font:14px Arial;color:#17231d}h1{color:#176b46;margin-bottom:4px}.meta{color:#64748b}.score{font-size:32px;font-weight:800;color:#176b46}.warning{padding:12px;background:#fff7db;border:1px solid #e7bd50}table{width:100%;border-collapse:collapse;margin-top:18px}th,td{padding:9px;border:1px solid #dbe3df;text-align:left;vertical-align:top}th{background:#edf7f1}.footer{margin-top:18px;font-size:11px;color:#64748b}</style></head><body><h1>FARPHA · Dossiê de pré-auditoria</h1><p class="meta">Gerado em ${esc(generatedAt)}</p><p class="score">${s.score}% de prontidão indicativa</p><p>${s.ready} áreas preparadas · ${s.attention} com atenção · ${s.notApplicable} não aplicáveis</p><p class="warning"><b>Aviso:</b> este documento organiza registos locais e não constitui auditoria oficial, certificação ou parecer jurídico/técnico.</p><table><thead><tr><th>Área</th><th>Verificação</th><th>Estado</th><th>Detalhe</th></tr></thead><tbody>${items.map(v=>`<tr><td>${esc(v.area)}</td><td>${esc(v.title)}</td><td>${esc(v.status)}</td><td>${esc(v.detail)}</td></tr>`).join("")}</tbody></table><p class="footer">Confirme os dados com os documentos originais, regras atuais, autoridades e profissionais competentes.</p></body></html>`}
export function buildAuditPackageTextFiles(items:AuditItem[],snapshot:unknown,generatedAt:string){const summary=summarizeAudit(items);return{"LEIA-ME.txt":`FARPHA — PACOTE DE PRÉ-AUDITORIA\nGerado em: ${generatedAt}\n\nEste pacote não constitui auditoria oficial ou certificação.\nInclui apenas dados e evidências guardados localmente neste dispositivo.\n`,"relatorio-auditoria.html":buildAuditReportHtml(items,generatedAt),"checklist-auditoria.csv":buildAuditCsv(items,generatedAt),"dados-tecnicos.json":JSON.stringify({generatedAt,summary,snapshot,items},null,2)}}
