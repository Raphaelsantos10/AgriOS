import{useMemo,useState}from"react";import{ArrowDownRight,ArrowRight,ArrowUpRight,History,Target}from"lucide-react";import{Link}from"react-router-dom";import Button from"../../../design-system/components/Button";import Card from"../../../design-system/components/Card";import{listAuditRuns,saveAuditRun}from"../services/auditHistoryStorage";import type{AuditItem}from"../utils/auditAssistant";import{auditPriorities,auditTrend,createAuditRun}from"../utils/auditHistory";
export default function ComplianceEvolutionCard({items}:{items:AuditItem[]}){const[history,setHistory]=useState(listAuditRuns),current=useMemo(()=>createAuditRun(items,new Date().toISOString()),[items]),trend=auditTrend(current,history),priorities=auditPriorities(items);function record(){setHistory(saveAuditRun(createAuditRun(items,new Date().toISOString())))}const TrendIcon=trend.direction==="improved"?ArrowUpRight:trend.direction==="declined"?ArrowDownRight:ArrowRight;return <div className="grid gap-6 xl:grid-cols-2"><Card className="p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="flex items-center gap-2 text-xl font-black"><History className="text-emerald-700"/> Evolução das avaliações</h2><p className="mt-1 text-sm text-slate-500">Histórico manual guardado neste navegador.</p></div><Button variant="secondary" onClick={record}>Guardar avaliação atual</Button></div><div className="mt-5 grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-emerald-50 p-4"><p className="text-xs text-slate-500">Atual</p><p className="text-3xl font-black text-emerald-700">{current.score}%</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">Anterior</p><p className="text-3xl font-black">{trend.previous===null?"—":`${trend.previous}%`}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">Variação</p><p className={`flex items-center gap-1 text-3xl font-black ${trend.delta>0?"text-emerald-700":trend.delta<0?"text-red-700":""}`}><TrendIcon/>{trend.previous===null?"—":`${trend.delta>0?"+":""}${trend.delta}`}</p></div></div><div className="mt-5 space-y-2">{history.slice(0,6).map(v=><div key={v.id} className="grid grid-cols-[1fr_auto_auto] items-center gap-3 rounded-lg border p-3 text-sm"><span>{new Date(v.createdAt).toLocaleString("pt-PT")}</span><span>{v.attention} atenção</span><b>{v.score}%</b></div>)}{history.length===0&&<p className="text-sm text-slate-500">Ainda não guardou nenhuma avaliação.</p>}</div></Card><Card className="p-5"><h2 className="flex items-center gap-2 text-xl font-black"><Target className="text-emerald-700"/> Prioridades recomendadas</h2><p className="mt-1 text-sm text-slate-500">Ordenação operacional das lacunas atuais; confirme a urgência real.</p><div className="mt-5 space-y-3">{priorities.map(v=><article key={v.id} className={`rounded-xl border p-4 ${v.priority==="high"?"border-amber-300 bg-amber-50":"border-slate-200"}`}><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-black uppercase text-slate-500">Prioridade {v.order} · {v.area}</p><h3 className="mt-1 font-black">{v.title}</h3><p className="mt-1 text-sm text-slate-600">{v.detail}</p></div><Link to={v.path} className="shrink-0 text-sm font-black text-emerald-700">Resolver</Link></div></article>)}{priorities.length===0&&<p className="rounded-xl bg-emerald-50 p-4 text-sm font-bold text-emerald-800">Nenhuma lacuna indicada pelos registos atuais. Confirme externamente antes de considerar a exploração preparada.</p>}</div></Card></div>}
