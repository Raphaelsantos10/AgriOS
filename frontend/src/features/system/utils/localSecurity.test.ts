import{describe,expect,it}from"vitest";import{assessLocalPassword,createLockConfig,decryptLocalBackup,encryptLocalBackup,verifyLockPassword}from"./localSecurity";describe("segurança local",()=>{it("exige palavra-passe longa e diversificada",()=>{expect(assessLocalPassword("fraca").valid).toBe(false);expect(assessLocalPassword("FARPHA-Segura-2026!").valid).toBe(true)});it("cria verificador sem guardar a palavra-passe",async()=>{const config=await createLockConfig("FARPHA-Segura-2026!");expect(JSON.stringify(config)).not.toContain("FARPHA-Segura-2026!");expect(await verifyLockPassword("FARPHA-Segura-2026!",config)).toBe(true);expect(await verifyLockPassword("Errada-Segura-2026!",config)).toBe(false)});it("cifra e autentica o backup com AES-GCM",async()=>{const encrypted=await encryptLocalBackup('{"farpha.harvest.v1":"[]"}',"Backup-Seguro-2026!","2026-07-21");expect(encrypted.ciphertext).not.toContain("harvest");expect(await decryptLocalBackup(encrypted,"Backup-Seguro-2026!")).toContain("harvest");await expect(decryptLocalBackup(encrypted,"Senha-Errada-2026!")).rejects.toThrow("incorreta")})});
