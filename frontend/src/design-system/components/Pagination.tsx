import { ChevronLeft, ChevronRight } from "lucide-react";
import { paginationItems } from "./paginationUtils";
export default function Pagination({ page, totalPages, onChange, label = "Paginação" }: { page: number; totalPages: number; onChange: (page: number) => void; label?: string }) { return <nav aria-label={label} className="flex items-center justify-center gap-1"><button type="button" disabled={page <= 1} onClick={() => onChange(page - 1)} className="farpha-touch-target grid place-items-center rounded-xl hover:bg-[var(--farpha-surface-muted)]" aria-label="Página anterior"><ChevronLeft size={18}/></button>{paginationItems(page, totalPages).map((item, index) => item === "ellipsis" ? <span key={`ellipsis-${index}`} className="grid h-11 w-8 place-items-center" aria-hidden="true">…</span> : <button type="button" key={item} aria-current={item === page ? "page" : undefined} onClick={() => onChange(item)} className={`h-11 min-w-11 rounded-xl px-2 text-sm font-semibold ${item === page ? "bg-[var(--farpha-brand-700)] text-white" : "hover:bg-[var(--farpha-surface-muted)]"}`}>{item}</button>) }<button type="button" disabled={page >= totalPages} onClick={() => onChange(page + 1)} className="farpha-touch-target grid place-items-center rounded-xl hover:bg-[var(--farpha-surface-muted)]" aria-label="Página seguinte"><ChevronRight size={18}/></button></nav>; }
