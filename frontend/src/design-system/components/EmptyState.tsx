import type { LucideIcon } from "lucide-react";
import Button from "./Button";
export default function EmptyState({ icon: Icon, title, description, actionLabel, onAction }: { icon: LucideIcon; title: string; description: string; actionLabel?: string; onAction?: () => void }) { return <div className="flex min-h-64 flex-col items-center justify-center rounded-2xl border border-dashed border-[var(--farpha-border-strong)] bg-[var(--farpha-surface-muted)] p-8 text-center"><span className="grid h-14 w-14 place-items-center rounded-2xl bg-[var(--farpha-brand-100)] text-[var(--farpha-brand-700)]"><Icon size={28}/></span><h3 className="farpha-heading-4 mt-5">{title}</h3><p className="mt-2 max-w-md text-sm leading-6 text-[var(--farpha-text-muted)]">{description}</p>{actionLabel && onAction ? <Button className="mt-6" onClick={onAction}>{actionLabel}</Button> : null}</div>; }
