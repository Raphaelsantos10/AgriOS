import type { ReactNode } from "react";
export default function FilterBar({ children, results }: { children: ReactNode; results?: number }) { return <section aria-label="Filtros" className="flex flex-col gap-3 rounded-2xl border border-[var(--farpha-border)] bg-[var(--farpha-surface)] p-4 shadow-[var(--farpha-shadow-sm)] lg:flex-row lg:items-end"><div className="grid min-w-0 flex-1 gap-3 sm:grid-cols-2 lg:flex lg:items-end">{children}</div>{results !== undefined ? <p role="status" className="shrink-0 pb-3 text-sm font-semibold text-[var(--farpha-text-muted)]">{results} resultado(s)</p> : null}</section>; }
