import { ChevronDown } from "lucide-react";
import type { ReactNode } from "react";
export type AccordionItem = { id: string; title: string; content: ReactNode };
export default function Accordion({ items, openId, onChange }: { items: AccordionItem[]; openId?: string; onChange: (id?: string) => void }) { return <div className="divide-y divide-[var(--farpha-border)] overflow-hidden rounded-2xl border border-[var(--farpha-border)]">{items.map(item => { const open = item.id === openId; return <section key={item.id}><h3><button type="button" aria-expanded={open} aria-controls={`accordion-${item.id}`} onClick={() => onChange(open ? undefined : item.id)} className="flex w-full items-center justify-between gap-4 p-4 text-left font-semibold hover:bg-[var(--farpha-surface-muted)]">{item.title}<ChevronDown className={`shrink-0 transition ${open ? "rotate-180" : ""}`} size={18}/></button></h3>{open ? <div id={`accordion-${item.id}`} className="border-t border-[var(--farpha-border)] p-4 text-sm leading-6 text-[var(--farpha-text-muted)]">{item.content}</div> : null}</section>; })}</div>; }
