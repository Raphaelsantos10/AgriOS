import type { ReactNode } from "react";
export type TabItem = { id: string; label: string; content: ReactNode; disabled?: boolean };
export default function Tabs({ items, value, onChange, label = "Secções" }: { items: TabItem[]; value: string; onChange: (value: string) => void; label?: string }) { const active = items.find(item => item.id === value) ?? items[0]; return <div><div role="tablist" aria-label={label} className="flex gap-1 overflow-x-auto border-b border-[var(--farpha-border)]">{items.map(item => <button key={item.id} type="button" role="tab" id={`tab-${item.id}`} aria-selected={item.id === active?.id} aria-controls={`panel-${item.id}`} disabled={item.disabled} onClick={() => onChange(item.id)} className={`shrink-0 border-b-2 px-4 py-3 text-sm font-semibold ${item.id === active?.id ? "border-[var(--farpha-brand-600)] text-[var(--farpha-brand-700)]" : "border-transparent text-[var(--farpha-text-muted)] hover:text-[var(--farpha-text)]"}`}>{item.label}</button>)}</div>{active ? <div role="tabpanel" id={`panel-${active.id}`} aria-labelledby={`tab-${active.id}`} tabIndex={0} className="py-4 outline-none">{active.content}</div> : null}</div>; }
