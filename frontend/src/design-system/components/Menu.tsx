import { useEffect, useRef, type ReactNode } from "react";
export type MenuItem = { id: string; label: string; icon?: ReactNode; danger?: boolean; disabled?: boolean; onSelect: () => void };
export default function Menu({ open, onClose, trigger, items, label }: { open: boolean; onClose: () => void; trigger: ReactNode; items: MenuItem[]; label: string }) { const ref = useRef<HTMLDivElement>(null); useEffect(() => { if (!open) return; const outside = (event: PointerEvent) => { if (!ref.current?.contains(event.target as Node)) onClose(); }; const key = (event: KeyboardEvent) => { if (event.key === "Escape") onClose(); }; document.addEventListener("pointerdown", outside); document.addEventListener("keydown", key); return () => { document.removeEventListener("pointerdown", outside); document.removeEventListener("keydown", key); }; }, [open, onClose]); return <div ref={ref} className="relative inline-flex">{trigger}{open ? <div role="menu" aria-label={label} className="absolute right-0 top-full z-[var(--farpha-z-dropdown)] mt-2 min-w-56 rounded-xl border border-[var(--farpha-border)] bg-[var(--farpha-surface)] p-1.5 shadow-[var(--farpha-shadow-lg)]">{items.map(item => <button key={item.id} role="menuitem" type="button" disabled={item.disabled} onClick={() => { item.onSelect(); onClose(); }} className={`flex w-full items-center gap-2 rounded-lg px-3 py-2.5 text-left text-sm font-semibold disabled:opacity-50 ${item.danger ? "text-[var(--farpha-danger-700)] hover:bg-[var(--farpha-danger-50)]" : "hover:bg-[var(--farpha-surface-muted)]"}`}>{item.icon}{item.label}</button>)}</div> : null}</div>; }
