import type { HTMLAttributes } from "react";
export default function Avatar({ name, src, size = "md", className = "", ...props }: HTMLAttributes<HTMLSpanElement> & { name: string; src?: string; size?: "sm" | "md" | "lg" }) { const dimensions = { sm: "h-8 w-8 text-xs", md: "h-10 w-10 text-sm", lg: "h-12 w-12 text-base" }; const initials = name.trim().split(/\s+/).slice(0, 2).map(item => item[0]).join("").toUpperCase(); return <span role="img" aria-label={name} className={`inline-grid shrink-0 place-items-center overflow-hidden rounded-full bg-[var(--farpha-brand-700)] font-bold text-white ${dimensions[size]} ${className}`} {...props}>{src ? <img src={src} alt="" className="h-full w-full object-cover"/> : initials}</span>; }
