import type { ReactNode } from "react";
export default function PageHeader({ eyebrow, title, description, actions, inverse = false }: { eyebrow?: string; title: string; description?: string; actions?: ReactNode; inverse?: boolean }) { return <header className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div>{eyebrow ? <p className={`text-xs font-extrabold uppercase tracking-[.18em] ${inverse ? "text-[#9cdf28]" : "text-[var(--farpha-brand-600)]"}`}>{eyebrow}</p> : null}<h1 className={`farpha-heading-1 mt-1 ${inverse ? "text-white" : ""}`}>{title}</h1>{description ? <p className={`mt-2 max-w-3xl text-sm leading-6 ${inverse ? "text-[#9daaa4]" : "text-[var(--farpha-text-muted)]"}`}>{description}</p> : null}</div>{actions ? <div className="flex flex-wrap gap-2">{actions}</div> : null}</header>; }
