import type { ButtonHTMLAttributes, ReactNode } from "react";
import { X } from "lucide-react";
export default function Chip({ children, selected = false, onRemove, ...props }: ButtonHTMLAttributes<HTMLButtonElement> & { children: ReactNode; selected?: boolean; onRemove?: () => void }) { return <span className={`inline-flex h-8 items-center gap-1 rounded-full border px-2.5 text-xs font-semibold ${selected ? "border-[var(--farpha-brand-600)] bg-[var(--farpha-brand-50)] text-[var(--farpha-brand-700)]" : "border-[var(--farpha-border)] bg-[var(--farpha-surface)]"}`}><button type="button" className="outline-none" aria-pressed={selected} {...props}>{children}</button>{onRemove ? <button type="button" onClick={onRemove} className="rounded-full p-0.5 hover:bg-black/5" aria-label={`Remover ${String(children)}`}><X size={13}/></button> : null}</span>; }
