FARPHA — CRIAR RELEASE v2.1.0 — PASSO OPCIONAL

Execute somente depois de a Sprint 50 estar publicada e FARPHA CI e FARPHA Security estarem verdes.

No Git Bash:

cd ~/Documents/Projetos/AgriOS
git status
git tag -a v2.1.0 -m "FARPHA v2.1.0"
git push origin v2.1.0

Depois abra:
https://github.com/Raphaelsantos10/AgriOS/actions

Abra FARPHA Release e confirme todos os passos verdes.
Na secção Artifacts, descarregue farpha-release-v2.1.0.

Se git status mostrar alterações pendentes, não crie a tag.
